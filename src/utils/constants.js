const ROLES = { // Creamos un objeto llamado ROLES que contendrá los diferentes tipos de roles de usuario.
    ADMIN: 1,   // Definimos el rol de ADMIN con el valor numérico 1.
    user: 2,    // Definimos el rol de usuario normal (user) con el valor numérico 2.
};

module.exports = ROLES; // Exportamos el objeto ROLES para que pueda ser usado en otros archivos del proyecto.
